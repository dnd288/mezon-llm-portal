#!/usr/bin/env sh
# Run lint-staged for pre-commit checks

set -eu

if command -v bun >/dev/null 2>&1; then
  bun x lint-staged
elif command -v npx >/dev/null 2>&1; then
  npx lint-staged
else
  echo "pre-commit: bun or npx is required to run lint-staged" >&2
  exit 1
fi
